# docker-compose root environment
# Copy to .env and fill in real values — never commit .env

DB_ROOT_PASSWORD=change_this_root_password
DB_NAME=soundsphere
DB_USER=soundsphere_user
DB_PASSWORD=change_this_db_password
